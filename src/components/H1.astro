---
type Props = {
  text: string;
};
const { text } = Astro.props;
---

<h1 class="text-6xl text-teal-900 font-bold mb-5">{text}</h1>
<p>
  &uarr; is H1.astro only used in [slug].astro so H1 text=text=post.data.title
</p>