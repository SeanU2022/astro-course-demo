---
import {type CollectionEntry, getEntryBySlug} from "astro:content"
import {Image} from "astro:assets"
// import xxx from "../images/speaker.jpg";

type Props = {
    post: CollectionEntry<'posts'>
}

const {post} = Astro.props
console.log("3 post.astro " + post.data.title)
---

<!-- width={600}
height={600 / 1.5} -->
<!-- class="rounded-2xl shadow-xl mb-6 aspect-thumbnail object-cover" -->

        <!-- <Image
            width={600}
            height={600 / 1.5}
            src={xxx} 
            alt={post.data.title}
            class="rounded-2xl shadow-xl mb-4 aspect-thumbnail object-cover"
            format="webp"
        /> -->
<article>
    <!-- USED TO BE height={1024 / 2} -->
    <!-- src={xxx} -->
    <a href={`/blog/${post.slug}`}>
        <Image
            src={post.data.image}            
            alt={post.data.title}
            width={1024}
            height={1024 / 1.5}
            format="webp"
            class="rounded-2xl shadow-xl mb-4 aspect-thumbnail object-cover"
        />
    </a>

    <a
        href={`/blog/${post.slug}`}
        class="text-4xl text-zinc-900 mb-4 font-semibold inline-block"
    >
        <h2> {post.data.title} </h2>
    </a>

    <p class="text-red-900 text-1xl mb-9">
        {post.data.author}
    </p>

    <p class="text-zinc-500 text-2xl mb-9 line-clamp-2">
        {post.body}
    </p>


</article>

